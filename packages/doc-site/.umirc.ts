import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'Qiniu JS SDK',
  favicon: '/favicon.ico',
  logo: '/logo.png',
  outputPath: 'dist',
  mode: 'site',
  // more config: https://d.umijs.org/config
});
